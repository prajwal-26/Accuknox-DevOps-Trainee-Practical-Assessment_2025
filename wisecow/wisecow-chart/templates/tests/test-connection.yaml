apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wisecow-chart.fullname" . }}-test-connection"
  labels:
    {{- include "wisecow-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wisecow-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
